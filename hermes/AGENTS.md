# My own instructions
Always be terse.

<!-- BEGIN hermes-obsidian (managed by install_obsidian.sh) -->
## Obsidian vault

The user's Obsidian vault "Main Notes" is on this machine at `/srv/obsidian`. It is a
real Obsidian vault kept in sync by the headless Sync client, so it is the same notes
the user reads on their phone and laptop.

Files you create or edit there sync to every device on the account within
seconds, so treat it as the user's live notes, not a scratch directory.

Read `/srv/obsidian/AGENTS.md` if it exists — it holds the user's own conventions for
how the vault is organised.
<!-- END hermes-obsidian -->
